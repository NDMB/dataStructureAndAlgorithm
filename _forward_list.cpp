// forward_list
// singly linked list 노드에 다음 노드의 주소만 저장 한방향으로만 순회함

// 한글 꺠진채로 인코딩 저장해버림; 다음부터는 그러지 않길...
#include <iostream>
#include <forward_list>

int main()
{
    std::forward_list<int> fwd_list = {1,2,3};

    fwd_list.push_front(0);

    auto it = fwd_list.begin(); // ?????¡Æ ?????? ????

    fwd_list.insert_after(it, 5); // it?? ???????? 5??? ?????? ????? ???¡Æ?

    fwd_list.insert_after(it, 6);

    fwd_list.pop_front(); // ??? ?? ??????? ????????? ????

    auto it1 = fwd_list.begin(); // it?? ??¡Æ??????????? ?????´? ??????? ¡Æ?????¡Æ??? ?¡?????

    fwd_list.erase_after(it1); // it ?????? ???? ????

    fwd_list.erase_after(it1, fwd_list.end()); // ?????????? ??????? ????

    std::forward_list<int> list1 = {23, 0, 1, -3, 34, 32};

    list1.sort(); // ????¡×?¡??? ??´?§?
    for(auto &c : list1)
    {
        std::cout << c << ' ';
    }
    std::cout << std::endl;


    list1.sort(std::greater<int>()); // ??????­?¡??? ??´?§?
    for(auto &c : list1)
    {
        std::cout << c << ' ';
    }
    std::cout << std::endl;

    std::forward_list<int> list2 = {2,53,1,0,4,10};

    list2.reverse(); // ??????????? ¡Æ??­??§?
    for(auto &c : list2)
    {
        std::cout << c << " ";
    }
    std::cout << std::endl;

    list2 = {0,1,0,1,-1,10,5,5,10,0};
    list2.unique();
    for (auto &c : list2)
    {
        std::cout << c << " ";
    }
    std::cout << std::endl;

    list2.sort(); // ???????? ??´?§?
    list2.unique(); // ????¨?? ¡Æ¡Æ????????? ????????? ?????
    for(auto c : list2)
    {
        std::cout << c << " ";
    }
    std::cout << std::endl;

    // ????´ ????¡Æ? ???§? ?? ?????????? 2???? ???? ???????? ????
    list2.unique([](int a, int b) 
    {
        return (b-a) < 2;
    });

    for (auto c : list2)
    {
        std::cout << c << " ";
    }
    std::cout << std::endl;
}